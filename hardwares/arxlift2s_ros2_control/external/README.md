# External dependencies for arxlift2s_ros2_control
#
# arx5-sdk/  — Stanford ARX5 SDK (symlink or copy). Required to build ArxX5Hardware.
# SOEM/      — OpenEtherCAT SOEM v1.4.0. Replaces conda-forge::soem.
#
# See ../README.md for setup commands.
